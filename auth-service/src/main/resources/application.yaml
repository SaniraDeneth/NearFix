spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5434/auth_db
    username: postgres
    password: "password"
    driver-class-name: org.postgresql.Driver
  jwt:
    secret: ${JWT_SECRET}
server:
  port: 8082