spring:
  application:
    name: gig-service
  datasource:
    url: jdbc:postgresql://localhost:5433/gig_db
    username: postgres
    password: "password"
    driver-class-name: org.postgresql.Driver

server:
  port: 8081